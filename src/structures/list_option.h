#ifndef LIST_OPTIONS
#define LIST_OPTIONS

enum class ListOption
{
    INSERT,
    UPDATE,
    REMOVE,
    FIND_BY_CHAR,
    SORT_BY_POINT,
    MAX_POINT,
    MIN_POINT,
    PRINT,
    EXIT
};

#endif
